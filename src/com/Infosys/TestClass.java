package com.Infosys;

public class TestClass {

    /*static List<Student> students = new ArrayList<>();

    public static void main(String[] args) {
        List<Student> = students.stream().filter(student -> student.getGrader() == "")
                .collect(Collectors.groupingBy(student.getGrader()));


      Map<String, List<Student>> gradeMap =  students.stream().collect(Collectors.groupingBy(student -> student.getGrade()))

    }*/
}

class Student{

        }


